# File: .env.local.example | Purpose: Document the short-lived database-owner input.
# Copy to .env.local locally; runtime code never loads this file.

DIRECT_URL=postgres://postgres:postgres@localhost:5432/nce_app
