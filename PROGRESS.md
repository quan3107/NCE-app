<!--
File: PROGRESS.md
Purpose: Track high-level progress updates for the English education app, split by backend and frontend.
Why: Provides shared visibility into recent dependency hardening work per project guidelines.
-->

# Progress Log

## Backend

- **2026-05-26:** Mapped backend architecture for a project mental map: Express app bootstrap, `/api/v1` router composition, route/controller/schema/service module boundaries, Prisma/Postgres RLS context, auth/session flow, background jobs, environment requirements, tests, and stale-doc/runtime risks. No backend source changes.
- **2026-05-26:** Added `docs/codebase-re-entry-tutorial-2026-05-26.md` with a backend-oriented re-entry map covering runtime commands, API/module boundaries, auth/RLS concepts, and a representative login flow. No backend source changes.
- **2026-05-26:** Split `backend/tests/modules/auth/auth.service.test.ts` from 572 to 289 lines by moving shared auth mocks/builders into `auth.service.test-utils.ts` and Google OAuth coverage into `auth.google.service.test.ts`. Verified the focused auth test suite and reran the backend build with dummy Brevo env values for test runs.
- **2026-05-26:** Split `backend/src/modules/submissions/submissions.service.ts` from 428 to 272 lines by moving IELTS timing/attempt guardrails into `submissions.timing.ts` and teacher notification enqueueing into `submissions.notifications.ts`. Verified backend build and focused submission/pending-count tests with dummy Brevo env values, then removed regenerated `backend/dist`.
- **2026-05-26:** Split `backend/src/modules/assignments/assignments.service.ts` from 327 to 241 lines by moving writing-rubric validation and student-facing sample-response filtering into `assignments.helpers.ts`. Verified backend build and focused assignment/pending-count tests with dummy Brevo env values, then removed regenerated `backend/dist`.
- **2026-05-26:** Split `backend/src/modules/auth/auth.controller.ts` from 316 lines into a 163-line controller plus a focused `auth.cookies.ts` helper module for refresh cookies, Google OAuth cookies, return URL validation, redirect URL resolution, and short OAuth error messages. Verified backend build, then reran focused auth/CMS route tests with dummy Brevo env values after the first run showed the local env was missing required email settings.
- **2026-05-26:** No backend files changed during the CSS cleanup pass; remaining backend bloat is in seed/schema/test/service/controller files.
- **2026-05-26:** No backend files changed during the teacher assignment detail cleanup pass; remaining backend oversized files still require deliberate domain-specific refactors.
- **2026-05-26:** No backend files changed during the teacher grading cleanup pass; backend large-file cleanup still requires targeted service/test/schema refactors rather than safe deletion.
- **2026-05-26:** No backend files changed during the IELTS create-page cleanup pass; backend large-file candidates are still pending deliberate service/test/schema-specific cleanup.
- **2026-05-26:** No backend files changed during the writing-form cleanup pass; backend cleanup remains limited to generated-output removal until a safe service/test/schema-specific refactor is selected.
- **2026-05-26:** No backend files changed during the listening-form cleanup pass; backend oversized files remain intentionally untouched until a safer feature-specific refactor is available.
- **2026-05-26:** Audited backend cleanup candidates for oversized files and generated clutter. Removed the ignored stale `backend/dist` build output, and left backend source unchanged because the large tracked files found were lockfiles, Prisma schema/seed data, or domain-heavy services/tests that need feature-specific refactors rather than safe deletion.
- **2026-05-25:** Created the `npm-security-updates-2026-05` branch for the safe npm update path using Node `v26.2.0` and npm `11.15.0`. Added backend lockfile tracking, updated Prisma packages to `7.8.0`, updated Express/runtime dependencies along non-major lines, added an `@hono/node-server@1.19.13` override to clear the Prisma dev-tooling audit path, and replaced an explicit `any` in the Prisma RLS delegate wrapper so lint has no errors.
- **2026-05-25:** No backend source changes for the repo-local Git Credential Manager update; this clone now defaults GitHub credentials to `quan3107` and suppresses GCM GUI prompts.

## Frontend

- **2026-05-26:** Mapped frontend architecture for a project mental map: Vite/React boot path, React Router route groups, public/authenticated shells, React Query API boundary, auth context bridge, feature modules, Tailwind token system, navigation cache/fallback behavior, tests, and UI risks. No frontend source changes.
- **2026-05-26:** Added `docs/codebase-re-entry-tutorial-2026-05-26.md` with a frontend-oriented re-entry map covering Vite runtime commands, routing, React Query/API client flow, auth context, and the current visual redesign state. No frontend source changes.
- **2026-05-26:** Removed the committed generated Tailwind output file `frontend/index.css`, switched the app to rely on `frontend/src/styles/globals.css` as the single Tailwind entry point, removed an expired commented CSS backup block, ignored local Factory/audit artifacts, and deleted the untracked `.factory/` screenshot/log cache plus regenerated `frontend/build` output.
- **2026-05-26:** Split the bloated `frontend/src/lib/ielts.ts` helper module into focused IELTS type, factory, question, text, and normalization modules while preserving the existing `@lib/ielts` import surface.
- **2026-05-26:** Split the oversized `frontend/src/components/ui/sidebar.tsx` into sidebar shell, context, group, and menu modules so each sidebar file stays under the 300-line project guideline while preserving the existing public exports.
- **2026-05-26:** Split `frontend/src/lib/auth.tsx` into provider, session-state, storage-mapping, and type modules so the shared auth files stay under the 300-line guideline while preserving `@lib/auth` exports.
- **2026-05-26:** Split `frontend/src/features/assignments/api.ts` into hook, mapper, request, and DTO modules so assignment data access stays under the 300-line guideline while preserving the existing hook exports.
- **2026-05-26:** Split `frontend/src/components/ui/chart.tsx` into chart rendering and chart context/style modules so the shared chart UI files stay under the 300-line guideline while preserving existing chart exports.
- **2026-05-26:** Split `frontend/src/features/courses/management/hooks/useTeacherCourseManagement.ts` into hook, mapper, and view-model type modules so course management state stays under the 300-line guideline while preserving the original hook/type import path.
- **2026-05-26:** Split `frontend/src/features/assignments/components/ielts/QuestionEditor.tsx` into editor shell, answer-controls, and pure transition-logic modules so the reusable IELTS question editor stays under the 300-line guideline while preserving its existing component API.
- **2026-05-26:** Split `frontend/src/features/assignments/components/ielts/MatchingEditor.tsx` into drag/drop orchestration and matching item/option rendering modules so the matching editor stays under the 300-line guideline while preserving its component API.
- **2026-05-26:** Split reading editor factory helpers out of `frontend/src/features/assignments/components/ielts/IeltsReadingContentEditor.tsx` so the inline reading content editor stays under the 300-line guideline while preserving its component API.
- **2026-05-26:** Split pure question creation/type-transition helpers out of `frontend/src/features/assignments/components/ielts/IeltsQuestionListEditor.tsx` so the reusable IELTS question-list editor stays under the 300-line guideline while preserving its component API.
- **2026-05-26:** Ran an in-app Browser smoke test against Vite after cleanup. Confirmed the homepage, course catalog, login route, protected teacher assignment redirect, and invalid-login alert render without runtime console errors; authenticated teacher/student flows still require a live API/auth session.
- **2026-05-26:** Split `frontend/src/features/assignments/components/ielts/authoring/ListeningAssignmentForm.tsx` into a 298-line orchestrator plus focused section, bulk-upload dialog, and helper modules. Verified frontend tests/build and reran the in-app Browser smoke for public routes and the protected teacher assignment redirect; authenticated form rendering still requires live auth/API.
- **2026-05-26:** Split `frontend/src/features/assignments/components/ielts/authoring/WritingAssignmentForm.tsx` from 538 lines into a 173-line orchestrator plus shared writing task, sample-response, and visual-upload components. Verified frontend tests/build, confirmed no new TypeScript errors in the split files, and reran the in-app Browser smoke for public routes and the protected assignment redirect.
- **2026-05-26:** Split `frontend/src/features/assignments/components/TeacherIeltsAssignmentCreatePage.tsx` from 447 lines into a 275-line route plus focused editor and draft/upload helper modules. Fixed the touched route's existing assignment-type payload cast, verified tests/build, confirmed `tsc` now only reports unrelated pre-existing errors, and reran the in-app Browser smoke for the public routes and unauthenticated create-route redirect.
- **2026-05-26:** Split `frontend/src/features/assignments/components/TeacherGradeFormPage.tsx` from 332 lines into a 158-line route plus grading panel and rubric helper modules. Verified frontend tests/build, confirmed TypeScript reports only unrelated pre-existing errors, and reran the in-app Browser smoke for public routes and the unauthenticated grade-route redirect.
- **2026-05-26:** Reduced `frontend/src/features/assignments/components/TeacherAssignmentDetailPage.tsx` from 323 to 271 lines by removing noisy debug logs and extracting header action controls. Verified frontend tests/build, confirmed TypeScript reports only unrelated pre-existing errors, and reran the in-app Browser smoke for public routes and the unauthenticated assignment-detail redirect.
- **2026-05-26:** Split `frontend/src/styles/globals.css` from 457 to 215 lines by moving IELTS authoring helpers, rich-text styles, and reading panel scroll utilities into focused CSS files imported after the Tailwind entry. Added Tailwind `@reference` directives where split styles use `@apply`, verified frontend tests/build, and reran the in-app Browser smoke to confirm routes render with expected body theme/font values and no console errors.
- **2026-05-25:** Created the `npm-security-updates-2026-05` branch for the safe npm update path using Node `v26.2.0` and npm `11.15.0`. Added frontend lockfile tracking, patched Vite from `6.3.5` to `6.4.2`, and kept major framework lines stable while regenerating the frontend lockfile.
- **2026-05-25:** Reimagined the main UI direction with a minimal sage-and-warm-neutral theme, refined shared buttons/cards/navigation chrome, updated the public homepage/course catalog/dashboard surfaces, and added a generated study-desk hero image under `frontend/public/assets/minimal-study-hero.png`.
- **2026-05-25:** No frontend source changes for the repo-local Git Credential Manager update; this clone now defaults GitHub credentials to `quan3107` and suppresses GCM GUI prompts.
