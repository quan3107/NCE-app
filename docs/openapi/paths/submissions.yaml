# File: docs/openapi/paths/submissions.yaml
# Purpose: Define submission OpenAPI path items.
# Why: Documents submission endpoints scoped to assignments.

AssignmentSubmissions:
  get:
    tags: [submissions]
    summary: List submissions for an assignment (admin/teacher; students see their own)
    parameters:
      - in: path
        name: assignmentId
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: Submissions
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '../schemas/submissions.yaml#/Submission'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
    security:
      - BearerAuth: []
  post:
    tags: [submissions]
    summary: Create submission (student)
    parameters:
      - in: path
        name: assignmentId
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../schemas/submissions.yaml#/CreateSubmissionRequest'
    responses:
      '201':
        description: Submission created
        content:
          application/json:
            schema:
              $ref: '../schemas/submissions.yaml#/Submission'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
    security:
      - BearerAuth: []

PendingSubmissionsCount:
  get:
    tags: [submissions]
    summary: Get ungraded submissions count (teacher/admin)
    description: Returns the count of ungraded (submitted or late, no grade) submissions for courses owned by the authenticated teacher/admin.
    responses:
      '200':
        description: Ungraded submissions count
        content:
          application/json:
            schema:
              type: object
              properties:
                count:
                  type: integer
                  description: Number of ungraded submissions
              required: [count]
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '403':
        description: Forbidden - Only teachers and admins can access this endpoint
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
    security:
      - BearerAuth: []

AssignmentSubmissionById:
  get:
    tags: [submissions]
    summary: Get submission (admin/teacher)
    parameters:
      - in: path
        name: assignmentId
        required: true
        schema:
          type: string
          format: uuid
      - in: path
        name: submissionId
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: Submission
        content:
          application/json:
            schema:
              $ref: '../schemas/submissions.yaml#/Submission'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '404':
        description: Not found
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
    security:
      - BearerAuth: []

SubmissionWritingAiFeedback:
  get:
    tags: [ai-feedback, submissions]
    summary: Get AI writing feedback draft status
    description: >
      Returns the latest AI writing feedback draft status for an authorized
      course teacher or admin. Active queued/running drafts include the same
      polling location returned by the manual generation request.
    parameters:
      - in: path
        name: submissionId
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: Latest writing feedback draft is terminal and readable to the requester.
        content:
          application/json:
            schema:
              $ref: '../schemas/ai-feedback.yaml#/WritingFeedbackResponse'
      '202':
        description: Latest writing feedback generation is queued or running.
        content:
          application/json:
            schema:
              $ref: '../schemas/ai-feedback.yaml#/WritingFeedbackResponse'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '403':
        description: Forbidden, or AI writing feedback is disabled by assignment policy.
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '404':
        description: No writing feedback draft exists for this submission.
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '409':
        description: Latest writing feedback draft is in a non-success terminal state.
        content:
          application/json:
            schema:
              $ref: '../schemas/ai-feedback.yaml#/WritingFeedbackResponse'
    security:
      - BearerAuth: []

SubmissionWritingAiFeedbackDrafts:
  get:
    tags: [ai-feedback, submissions]
    summary: List AI writing feedback draft history
    description: >
      Returns non-deleted AI writing feedback drafts for an authorized course
      teacher or admin, including rejected drafts retained for audit.
    parameters:
      - in: path
        name: submissionId
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: Draft history for the submission.
        content:
          application/json:
            schema:
              $ref: '../schemas/ai-feedback.yaml#/WritingFeedbackHistoryResponse'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
    security:
      - BearerAuth: []

SubmissionWritingAiFeedbackRegenerate:
  post:
    tags: [ai-feedback, submissions]
    summary: Regenerate AI writing feedback
    description: >
      Queues a replacement AI writing feedback draft after teacher review,
      optionally overriding the provider tier for this regeneration only.
    parameters:
      - in: path
        name: submissionId
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: false
      content:
        application/json:
          schema:
            $ref: '../schemas/ai-feedback.yaml#/WritingFeedbackRegenerateRequest'
    responses:
      '200':
        description: Regenerated draft is already terminal.
        content:
          application/json:
            schema:
              $ref: '../schemas/ai-feedback.yaml#/WritingFeedbackResponse'
      '202':
        description: Regenerated draft is queued or running.
        headers:
          Location:
            description: Polling location for the regenerated draft.
            schema:
              type: string
        content:
          application/json:
            schema:
              $ref: '../schemas/ai-feedback.yaml#/WritingFeedbackResponse'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '403':
        description: Forbidden or AI writing feedback is disabled by assignment policy.
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '409':
        description: Feedback cannot be regenerated because another draft is active or the submission is not ready.
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
    security:
      - BearerAuth: []

SubmissionWritingAiFeedbackApprove:
  post:
    tags: [ai-feedback, submissions]
    summary: Approve AI writing feedback into final grade feedback
    description: >
      Publishes teacher-edited AI feedback by atomically updating the existing
      grade feedback and recording the approved draft decision.
    parameters:
      - in: path
        name: submissionId
        required: true
        schema:
          type: string
          format: uuid
      - in: path
        name: draftId
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../schemas/ai-feedback.yaml#/WritingFeedbackApprovalRequest'
    responses:
      '200':
        description: Draft approved and grade feedback updated.
        content:
          application/json:
            schema:
              $ref: '../schemas/ai-feedback.yaml#/WritingFeedbackReviewResponse'
      '400':
        description: Invalid teacher feedback or criterion suggestions.
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '404':
        description: Draft not found
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '409':
        description: Draft is not decidable, already decided, or no grade exists.
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
    security:
      - BearerAuth: []

SubmissionWritingAiFeedbackReject:
  post:
    tags: [ai-feedback, submissions]
    summary: Reject an AI writing feedback draft
    description: Marks a draft rejected with an optional teacher reason while retaining it for audit.
    parameters:
      - in: path
        name: submissionId
        required: true
        schema:
          type: string
          format: uuid
      - in: path
        name: draftId
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: false
      content:
        application/json:
          schema:
            $ref: '../schemas/ai-feedback.yaml#/WritingFeedbackRejectRequest'
    responses:
      '200':
        description: Draft rejected.
        content:
          application/json:
            schema:
              $ref: '../schemas/ai-feedback.yaml#/WritingFeedbackReviewResponse'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '404':
        description: Draft not found
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '409':
        description: Draft is not decidable or has already been decided.
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
    security:
      - BearerAuth: []

SubmissionWritingAiFeedbackFinalize:
  post:
    tags: [ai-feedback, submissions]
    summary: Finalize provisional instant-visible AI writing feedback
    description: >
      Replaces provisional instant-visible AI feedback with teacher-final grade
      feedback by atomically updating the existing grade and draft decision.
    parameters:
      - in: path
        name: submissionId
        required: true
        schema:
          type: string
          format: uuid
      - in: path
        name: draftId
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../schemas/ai-feedback.yaml#/WritingFeedbackApprovalRequest'
    responses:
      '200':
        description: Draft finalized and grade feedback updated.
        content:
          application/json:
            schema:
              $ref: '../schemas/ai-feedback.yaml#/WritingFeedbackReviewResponse'
      '400':
        description: Invalid teacher feedback or criterion suggestions.
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '404':
        description: Draft not found
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '409':
        description: Draft is not decidable, already decided, or no grade exists.
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
    security:
      - BearerAuth: []

  post:
    tags: [ai-feedback, submissions]
    summary: Request AI writing feedback draft generation
    description: >
      Allows an authorized course teacher or admin to manually request or
      regenerate AI feedback for a submitted IELTS writing response. Drafts
      remain separate from final grade feedback; teacher-reviewed drafts stay
      hidden from students, while instant-student-visible drafts are provisional.
      Visual Task 1 requests require backend-validated hosted image context or
      return a teacher-review-only status explaining why image context is
      unavailable.
    parameters:
      - in: path
        name: submissionId
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: Existing or regenerated writing feedback draft is already terminal and readable to the requester.
        content:
          application/json:
            schema:
              $ref: '../schemas/ai-feedback.yaml#/WritingFeedbackResponse'
      '202':
        description: Writing feedback generation has been queued or is running.
        headers:
          Location:
            description: Polling location for the writing feedback draft request.
            schema:
              type: string
        content:
          application/json:
            schema:
              $ref: '../schemas/ai-feedback.yaml#/WritingFeedbackResponse'
      '400':
        description: Submission is not a writing assignment or request parameters are invalid.
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '403':
        description: Forbidden or AI writing feedback is disabled by assignment policy.
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '409':
        description: Feedback cannot be queued because the submission is not ready, a duplicate draft is active, or review is required.
        content:
          application/json:
            schema:
              oneOf:
                - $ref: '../schemas/ai-feedback.yaml#/WritingFeedbackResponse'
                - $ref: '../schemas/common.yaml#/ErrorResponse'
      '413':
        description: AI writing feedback input exceeds the configured maximum size.
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
      '503':
        description: AI feedback generation is disabled globally or provider configuration is unavailable.
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ErrorResponse'
    security:
      - BearerAuth: []
