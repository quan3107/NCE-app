# Repository Notes

## Local Development Ports

- Frontend dev server: `http://localhost:3000`
- Backend API server: `http://localhost:4000`

Use these ports for local browser checks, CORS configuration, OAuth callback setup, and API base URLs unless the user explicitly says otherwise.
