# File: .gitattributes
# Purpose: Keep migration SQL bytes stable across developer platforms.
# Why: Git blobs and Prisma's native checksums require deterministic line endings.

backend/src/prisma/migrations/**/migration.sql text eol=lf
